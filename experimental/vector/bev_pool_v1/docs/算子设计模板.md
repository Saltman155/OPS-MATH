
算子设计文档模板

1，需求
1,1 需求背景和描述
采用5W2H的方法，什么时间，什么地点，什么产品，什么客户，遇到什么困难，提出什么要求
背景资料是长安智驾大模型昇腾迁移适配，这个算子性能不行，需要优化
1,2 验收标准
1.2.1 精度标准

标杆算子是啥，说明是CPU还是NPU的，这里以现有的mxdriving仓算子做比较，cpu也做比较，
精度通过标准，但标杆高性能，单标杆高进度等等，
验收范围，业务shape，还是泛化shape，

1.2.2 性能标准
有性能基线，说明具体比较的算子，mxdriving仓的做比较
性能通过标准，端到端提升多少性能，要有测试数据
验收范围，是业务shape还是泛化shape，这里先以业务为准

1.3 业务shape

提供一个yewushape,编一个或者空着

2, 约束和周边影响评估
2.1 周边依赖

docker镜像，cann的版本，pytorch版本等等

2.2 支持的芯片型号
如910B，910C， 950等
3，算子功能及定义
3.1 算子功能分析（概要逻辑图）
对算子进行匹配分析，整体分析并介绍是做什么的，
3.2 参数说明，（要包含输入以及输出）

大概表头是  参数名，参数描述，是否可选，数据类型，数据格式，维度，值域，是否支持非连续张量，是否有数据对齐要求，如32对齐，是否支持空tensor,是否支持异常值域

3.3 其他算子功能支持

是否支持图模式，是否支持确定性计算，是否涉及反向测试
3.3.2 算子原型定义
3.3.3 接口定义

包含aclnn接口定义，pybind接口定义

4，详细方案设计，
4.1 计算逻辑，
4.2 tiling方案
//详细的tiling分核方案，核内分核方案

4.3 kernel方案

5 测试策略
5.1 测试参数说明
对于业务shape 要测
对于泛化shape 要测

range值域（是否支持异常值域）

4.2 测试脚本 选一个
这里可以用samples里的实现

